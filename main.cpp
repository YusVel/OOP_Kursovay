#include "mainwindow.h"
#include "VECTOR.h"
#include <QApplication>

int main(int argc, char *argv[])
{
   // QApplication a(argc, argv);
   // MainWindow w;

    VECTOR<int> v(10);
    auto it = v.begin();

   // w.show();
   // return a.exec();
    return 0;
}
